module MerchantsHelper
	# should I return the entry for merchant name here?
	# why do I have to when sample_app does not?
	# def input_box_text(input_text = '')
	# if input_text.empty?
	#  "Enter Merchant"
	# else
	#  input_text
	# end
	# then in the edit page, provide :input_text = @merchant.name
end
